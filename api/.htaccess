<IfModule mod_rewrite.c>
  Options -Indexes
  Options -Multiviews
</IfModule>


<IfModule mod_rewrite.c>

  Options -Indexes

  Options -Multiviews

  RewriteEngine On

  RewriteBase /badal/api

</IfModule>
